# Benchmarks

Benchmarking is deferred until representative HSD-generated DNSSEC responses are pinned. Parser
limits are part of the public API so future allocation and latency gates can use identical inputs.

